# Copy to .env on the deploy host. Never commit real values.
ADDR=127.0.0.1:18280
PUBLIC_ORIGIN=https://music.52131415.xyz
STATIC_DIR=./frontend/dist
DATA_DIR=./data

# Required: private upstream base URL (local/Tailnet only). Keep off git.
COCO_PLAY_BASE=
# Optional public origin for the same upstream, if any.
UPSTREAM_PUBLIC=
